/**
 * Chat Tab Screen
 * Displays chat/messaging screen (accessible via navigation)
 */

import React from 'react';
import { ChatScreen } from '@/src/screens/shared';

export default function Chat() {
  return <ChatScreen />;
}
