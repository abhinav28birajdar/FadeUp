import { Redirect } from 'expo-router';

export default function Index() {
    // Navigation is handled by RootLayoutNav
    // This just gives a placeholder while _layout resolves auth
    return null;
}
